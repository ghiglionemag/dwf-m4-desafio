function main() {
  const header= document.querySelector(".header");
  headerComponent(header);

  const contact = document.querySelector(".contactame");
  contactComponent(contact);

  const footer = document.querySelector(".footer");
  componetFooter(footer);
}
main();
